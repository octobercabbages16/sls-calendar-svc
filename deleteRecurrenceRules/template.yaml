AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Calendar Service - Delete Recurrence Rules

Globals:
  Function:
    Timeout: 30
    Runtime: nodejs20.x
    MemorySize: 256

Parameters:
  Environment:
    Type: String
    Default: dev
    AllowedValues:
      - dev
      - staging
      - prod
  SecretName:
    Type: String
    Description: Name of the Secrets Manager secret for DB credentials

Resources:
  DeleteRecurrenceRules:
    Type: AWS::Serverless::Function
    Properties:
      FunctionName: !Sub deleteRecurrenceRules-${Environment}
      Handler: dist/deleteRecurrenceRules.handler
      CodeUri: ./
      Environment:
        Variables:
          SECRET_NAME: !Ref SecretName
      Policies:
        - AWSSecretsManagerGetSecretValuePolicy:
            SecretArn: !Sub arn:aws:secretsmanager:${AWS::Region}:${AWS::AccountId}:secret:${SecretName}*
      Events:
        DeleteRecurrenceRulesApi:
          Type: Api
          Properties:
            Path: /recurrence-rules/{id}
            Method: DELETE

Outputs:
  DeleteRecurrenceRulesFunctionArn:
    Description: ARN of the Delete Recurrence Rules Lambda function
    Value: !GetAtt DeleteRecurrenceRules.Arn
  ApiEndpoint:
    Description: API Gateway endpoint URL
    Value: !Sub https://${ServerlessRestApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/recurrence-rules/{id}
