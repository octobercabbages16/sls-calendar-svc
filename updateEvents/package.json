{
  "name": "update-events",
  "version": "1.0.0",
  "description": "Lambda function to update events",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "invoke": "sam local invoke UpdateEvents --env-vars env/dev.json --event events/event-update-event.json --template template.yaml",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "pg": "8.13.1",
    "knex": "^2.1.0"
  },
  "devDependencies": {
    "@aws-sdk/client-secrets-manager": "3.650.0",
    "@types/aws-lambda": "8.10.145",
    "@types/pg": "8.11.10",
    "typescript": "5.6.3"
  }
}
